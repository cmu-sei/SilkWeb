Here are some examples for integrating this to FastCGI or SCGI 
and have a number of child processes loaded in memory with SilkAPI 
instance for reuse.  If you use MPI cluster you can do a basic breakup 
of the work between multiple workers and do loadbalancing that way.

